import { PagePlaceholder } from "@/components/page-placeholder";

const FaqPage = () => {
	return (
		<PagePlaceholder
			description="Answers to common questions about onboarding, integrations, billing, and candidate data."
			title="Frequently Asked Questions"
		/>
	);
};

export default FaqPage;
