import { PagePlaceholder } from "@/components/page-placeholder";

const FeaturesPage = () => {
	return (
		<PagePlaceholder
			description="Overview of Recruitify's builder, analytics, domain management, and candidate workflow."
			title="Platform Features"
		/>
	);
};

export default FeaturesPage;
