import { PagePlaceholder } from "@/components/page-placeholder";

const AgenciesPage = () => {
	return (
		<PagePlaceholder
			description="Switch between agencies, create new workspaces, and manage invitations."
			title="Agencies"
		/>
	);
};

export default AgenciesPage;
