import { PagePlaceholder } from "@/components/page-placeholder";

const AnalyticsTrafficPage = () => {
	return (
		<PagePlaceholder
			description="Break down visits by channel, location, and UTM parameters."
			title="Traffic Insights"
		/>
	);
};

export default AnalyticsTrafficPage;
