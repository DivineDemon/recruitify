import { PagePlaceholder } from "@/components/page-placeholder";

const ApplicationsPage = () => {
	return (
		<PagePlaceholder
			description="Centralized applicant pipeline with filters, bulk actions, and exports."
			title="Applications"
		/>
	);
};

export default ApplicationsPage;
