import { PagePlaceholder } from "@/components/page-placeholder";

const JobsPage = () => {
	return (
		<PagePlaceholder
			description="Manage open roles, track publishing status, and sync with templates."
			title="Jobs"
		/>
	);
};

export default JobsPage;
