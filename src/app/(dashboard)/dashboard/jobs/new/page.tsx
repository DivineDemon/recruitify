import { PagePlaceholder } from "@/components/page-placeholder";

const NewJobPage = () => {
	return (
		<PagePlaceholder
			description="Draft a new job post, define requirements, and connect it to templates."
			title="Create Job"
		/>
	);
};

export default NewJobPage;
