import { PagePlaceholder } from "@/components/page-placeholder";

const BillingPage = () => {
	return (
		<PagePlaceholder
			description="Show current plan, usage limits, payment method, and upcoming invoices."
			title="Billing"
		/>
	);
};

export default BillingPage;
