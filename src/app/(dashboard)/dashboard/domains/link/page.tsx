import { PagePlaceholder } from "@/components/page-placeholder";

const LinkDomainPage = () => {
	return (
		<PagePlaceholder
			description="Step-by-step wizard for attaching a custom domain or selecting a Recruitify subdomain."
			title="Connect Domain"
		/>
	);
};

export default LinkDomainPage;
