import { PagePlaceholder } from "@/components/page-placeholder";

const DomainsPage = () => {
	return (
		<PagePlaceholder
			description="View free subdomains, custom domain connections, and DNS verification statuses."
			title="Domains"
		/>
	);
};

export default DomainsPage;
