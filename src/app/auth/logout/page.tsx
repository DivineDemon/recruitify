import { PagePlaceholder } from "@/components/page-placeholder";

const LogoutPage = () => {
	return (
		<PagePlaceholder
			description="Clear the local session and redirect the user back to the marketing site."
			title="Signing Out"
		/>
	);
};

export default LogoutPage;
